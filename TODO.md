# TODO
- [ ] Create multi-page structure: keep `index.html` as landing (hero) + footer only.
- [ ] Create `services.html`, `work.html`, `process.html`, `about.html`, `contact.html` with shared header/footer.

- [ ] Update nav links to route to pages (e.g., `services.html`) instead of `#anchors`.
- [ ] Update CTAs in hero/work tiles to navigate to correct pages.
- [ ] Update `js/script.js` if needed so scroll-reveal + mobile burger work across all pages.
- [ ] Ensure WhatsApp prefilled form works on `contact.html`.
- [ ] Remove unused anchor-based reveal IDs/links from `index.html`.
- [ ] Test by opening each HTML page and validating navigation.

